#include <bits/stdc++.h>
using namespace std;

int t;

int main(void) {
    cin >> t;

    while (t--) {
        int Sm, Sc, Si;
        cin >> Sm >> Sc >> Si;
        vector<char> commands(Sc);
        for (int i = 0; i < Sc; i++) {
            cin >> commands[i];
        }

        vector<char> v(Si);

        int pointer = 0;
    }
}